<h1>
  Payment Results
</h1>

<dl>
  <dt>APR</dt>
  <dd><%= format("%.4f", params.fetch("user_apr").to_f) %>%</dd>
  
  <dt>Number of years</dt>
  <dd><%= params.fetch("user_years") %></dd>

  <dt>Principal</dt>
  <dd>$<%= format("%.2f", params.fetch("user_pv").to_f) %></dd>

  <dt>Monthly payment</dt>
  <dd>$<%= format("%.2f", @monthly_pmt) %></dd>
</dl>

<a href="/payment/new">Calculate another payment</a>
